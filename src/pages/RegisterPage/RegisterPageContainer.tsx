import React, { FC } from 'react';
import RegisterPage from './RegisterPage';

const RegisterPageContainer: FC<any> = function () {
  return <RegisterPage />;
};

export default RegisterPageContainer;
